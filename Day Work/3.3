第十四天
      昨天没有写随笔 因为上午洗澡  下午才来实验室 又收到毕业相关材料
      
      
      深度学习基础--循环神经网络
                   RNN是处理序列信息的--输入和输出是有关系的
                   语言模型：给定一个一句话前面的部分，预测接下来最有可能的一个词是什么
                   权重矩阵 W 就是隐藏层上一次的值作为这一次输入的权重
                   双向RNN-隐藏层需要保存两个值（1.参与正向计算2.参与反向计算）计算时不共享权重
                   深度循环神经网络
      
      机器学习专题--集成学习
                   为什么使用决策树作为基学习器：
                         (1). 决策树的表达能力和泛化能力，可以通过剪枝快速调整；
                         (2). 决策树可以方便地将**样本的权重**整合到训练过程中；
                         (3). 决策树是一种**不稳定**的学习器； 所谓不稳定，指的是数据样本的扰动会对决策树的结果产生较大的影响；
                   神经网络也可以作为基学习器
                   Boosting 能提升弱分类器性能的原因是降低了偏差；Bagging 则是降低了方差
                   adaBoost分类器利用同一种基分类器（弱分类器），基于分类器的错误率分配不同的权重参数，最后累加加权的预测结果作为输出。
                   Bagging--基于数据随机重抽样的分类器构建方法
      
      基础算法--搜索算法---解决最短路径问题
               DFS-树深度求解和BFS--二叉树的层序遍历
               DFS--深度优先顾名思义，就是寻找某种状态的时候选择一条路走到底，走不通就退回去换另一条路。
                    --隐含使用了栈的结构
               BFS--广度优先搜索则是横向搜索，当到达一个状态后，下一次同时走与这个状态相关联的状态，一层一层这样递进，直到找不到下一个状态或目标状态。
                   --求解--1.节点与节点之间有无路径 2.求节点间的最短距离
                           使用了队列结构
               迪杰斯特拉算法--是一种映射关系  贪婪算法 ---中心思想：让每个节点记录它到起点的最短路径
                   ---每次只找离起点最近的一个结点，并将之并入已经访问过结点的集合（以防重复访问，陷入死循环），
                      然后将刚找到的最短路径的结点作为中间结点来更新相邻结点的路径长度，这样循环找到图中一个个结点的最短路径。
                      
                针对树形结构求解类似最短路径或最小权重和的题目时，DFS和BFS是不错的选择，
                但是当遇到有环状结构的最短路径题时应该格外警惕，此时选择DijKstra算法比较稳妥。
                
       tensorflow--Linear Regression线性回归
       
