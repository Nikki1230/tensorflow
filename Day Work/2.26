第十天
     基础算法--哈希
              哈希集合--get() 存储不重复值的数据结构
              哈希映射--put(key, value) 用存储键值对的一种实现
     
     机器学习-实践
             简述一个机器学习的项目过程--抽象数学问题  获取数据  预处理与特征选择  模型训练与调优 模型诊断
             模型度量指标
             数据清洗和特征处理
             关联规则度量指标-支持度、置信度、提升度
     
     python视频-tcp编程-比udp更安全-可靠传输
                tcp采用应答机制  超时重传 错误校验 
                创建套接字
                连接服务器  .connect(IP地址，port)
                收发数据 .send(数据.encode("UTF-8"))
                关闭套接字
     
     深度学习--线性单元和梯度下降
            目标函数（误差）--（真-预测）的平方
     
              课本上-点估计是指输入和目标变量之间关系的估计。
              均方误差-衡量偏差与方差一起评估模型
              最大似然估计--从模型函数中选择特定函数进行估计 然后分析偏差和方差
              贝叶斯估计--先验概率
     
     python315道面试题--前30道
                     1字节=8 位
                     and  or 计算规则
                     机器码，学名机器语言指令，有时也被称为原生码，是电脑的CPU可直接解读的数据。
                     字节码是一种中间状态（中间码）的二进制代码（文件）。需要直译器转译后才能成为机器码。
                     is  比较的是内存地址
                     == 比较的是值
                      结果+　if  + 条件  + else + 结果
                      map(func，iterable)根据提供的函数对指定序列做映射。
                      当前函数引用到上一层函数的局部命名空间的变量时就会触发闭包规则
     
     tensorflow代码-Random Forest 
                   决策树也是一个难点  -- ID3-信息增益  C4.5-信息增益率  CART-基尼系数 都是生成决策树的算法
                   将使用CART决策树作为弱学习器的Bagging方法称为随机森林。
     
