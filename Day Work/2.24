第八天
     python视频--udp编程
                .sendto(发送的信息，地址ip和port-元组)
                encode('UTE-8') 将字符串转换为字节
                .bind(本地，port) 绑定端口信息
                scoket套接字是全双工--在同一时刻 可以双向传递数据
     
     深度学习基础--递归神经网络
                  可以处理如树、图的递归结构
                  作用：利用树、图结构的模型处理信息，模型能够区分两个不同的意思
                  语义向量的远近
                  递归神经网络的输入是树/图结构，而这种结构需要花费很多人工去标注。
                  自然语言和自然场景运用
     
     DLinterview-基础算法
                 查找
                 二分查找--在查找表中不断取中间元素与查找值进行比较，以二分之一的倍率进行表范围的缩小。
                 二叉排序树--为了提高查找和插入删除关键字的速度。 查找 插入 删除
                 平衡二叉树--其每一个节点的左子树和右子树的高度差最多等于1。
                 B树--其每一个节点的孩子可以多于两个，且每一个结点处可以存储多个元素。
     深度学习课本-容量、拟合
     
     tensorflow--Nearest Neighbor(最近邻算法)
                 选择样本数据集中前k个最相似的数据
                 numpy中shape[0]返回数组的行数，shape[1]返回列数
                 L1 distance 曼哈顿距离-- 实际的驾驶距离
                 L2 distance 欧氏距离--初高中常用的两点之间的距离
