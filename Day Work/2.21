第五天
     屡试不爽的套路，先研究一个特殊的情形，再扩展为一般规律。
     深度学习相关--卷积神经网络
                  特点：局部连接、权值共享、下采样
                  卷积计算公式中  符号句号表示element-wise product，即将矩阵中每个对应元素相乘。
                  Pooling层需要做的仅仅是将误差项传递到上一层，而没有梯度的计算。
                  代码不理解 搞不懂每一个之间的连接
     DLinterview--基础算法相关编程题
     python视频文档--异常、文件和包
                    try  except--在开发中，可以在主函数中增加 异常捕获
                    else 只有在没有异常时才会执行的代码
                    finally 无论是否有异常，都会执行的代码--还可以根据 应用程序 特有的业务需求 主动抛出异常
                    文件的读取
                    字符串编码 - UTF-8 Unicode
                    eval() 将字符串当成有效的表达式来求值并返回计算结果
     tensorflow-回归的实现
                机器学习关注待解决的问题是什么类型的问题 分类or回归  深度学习方法可以解决这种问题
                机器学习中主要是各种算法
     
     深度学习课本（中文版）--机器学习基础
     
     毕设4.2勉强完成  -- 5.1写个开头
     
     Tensorflow--Eager API(即时执行)--tfe.enable_eager_execution()
                 与正常相比，不需要启动默认图 就可以直接用Tensor进行计算(tf.Session)
